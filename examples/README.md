# GoldenPipe Examples

| Script | Description |
|--------|-------------|
| `basic_pipeline.py` | Run a full check -> flow -> match pipeline on a CSV |
| `selective_stages.py` | Run only check + flow stages, skipping match |

## Prerequisites

```bash
pip install goldenpipe goldencheck goldenflow goldenmatch
```
